#include "eduStruct.h"

extern void show_modules_info(etudiant_info* );

extern void add_module(etudiant_info* );

extern void edit_module(etudiant_info* );

extern void delete_module(etudiant_info* );

extern void  modifier_modules_info(etudiant_info* );

extern void modifier_etudiant(etudiant_info* );

extern etudiant *modifier_etud_info(etudiant*  , int );
